# cpp-sailing_performance_analyzer
Parses CSV sailing data into structs (Date/Time/Sailing), reports invalid lines, then aggregates late-performance by route and by day. Demonstrates input parsing, error handling, vector-based aggregation, and command line arguments.
